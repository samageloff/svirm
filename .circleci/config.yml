version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Howdy folks.
      - run:
          name: Print the Current Time
          command: date
      - run:
          name: Running tests
          command: npm test